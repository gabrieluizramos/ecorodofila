const users = new Map([
  ['user', 'password'],
  ['admin', 'admin']
]);

const tokens = new Map([
  ['admin', '123token']
]);

const names = new Map([
  ['admin', 'Gabriel Ramos']
]);

module.exports = {
  tokens,
  users,
  names
};
